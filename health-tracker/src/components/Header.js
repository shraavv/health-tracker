import React from 'react';

const Header = () => {
  return (
    <header className="text-center">
      <nav class="navbar">
        <h1>Health Tracker</h1>
      </nav>
    </header>
  );
};

export default Header;
